name: Introduction

on: push

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            -   name: say hello
                run: echo "Hello World"
    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            -   name: Successful step
                run: | 
                    echo "I will succeed"
            -   name: Say goodbye
                run: echo "Goodbye!"